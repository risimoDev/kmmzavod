# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Data validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Storage (MinIO / S3-compatible)
aioboto3==13.1.1

# System metrics (used in /metrics endpoint)
psutil==6.0.0

# Speech-to-text (transcript-driven moment selection + subtitles), RU
faster-whisper==1.1.0

# Beat detection / audio energy envelope for rhythm-aware cutting
librosa>=0.10.0
soundfile>=0.12.0

# Light CV: face detection (OpenCV Haar cascade for smart-crop), motion, phash
opencv-python-headless>=4.8.0
numpy>=1.24.0,<2.0.0

# LLM moment selection + vision via GPTunnel (OpenAI-compatible)
httpx>=0.27.0

# CLI
click==8.1.7

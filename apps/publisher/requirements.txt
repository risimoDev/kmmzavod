# Minimal pins only. instagrapi / tiktok-uploader pin their own exact versions of
# pydantic, requests, selenium, Pillow, moviepy — pinning those here just causes
# ResolutionImpossible conflicts. Let the two heavy libs drive those transitively.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic-settings>=2.4,<3

# Instagram private API (pure HTTP) — pulls pydantic==2.7.1, requests, Pillow, etc.
instagrapi==2.1.2

# TikTok upload (Selenium + headless Chromium) — pulls selenium, etc.
tiktok-uploader==1.0.2

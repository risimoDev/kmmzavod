# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# HTTP (download video from MinIO presigned URL)
requests==2.32.3

# Instagram private API (pure HTTP, no browser).
# instagrapi pulls its own compatible moviepy/Pillow — don't pin them here.
instagrapi==2.1.2

# TikTok upload (Selenium + headless Chromium)
tiktok-uploader==1.0.2
selenium==4.25.0
